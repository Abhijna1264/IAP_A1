part - 6 
Optional: Check Redis data

Exec into the Redis container:

docker exec -it docker-redis-1 redis-cli


Run:

KEYS *
HGETALL users:user1
HGETALL sessions
SMEMBERS room:lobby


You can see all user authentication info, active sessions, and rooms.

part 7: openssl s_client -connect localhost:65432

part-8: From docker/ folder:

docker-compose up --build

new terminal : docker ps
CONTAINER ID   IMAGE            COMMAND                  CREATED          STATUS                   PORTS  
                                           NAMES
cb163c05af1d   docker-server1   "python server/serve…"   3 minutes ago    Up 3 minutes             0.0.0.0:65432->65432/tcp, [::]:65432->65432/tcp   docker-server1-1
41f8274a3d36   docker-server2   "python server/serve…"   3 minutes ago    Up 3 minutes             0.0.0.0:65433->65432/tcp, [::]:65433->65432/tcp   docker-server2-1
4e6f118a29b9   redis:7          "docker-entrypoint.s…"   10 minutes ago   Up 3 minutes (healthy)   0.0.0.0:6379->6379/tcp, [::]:6379->6379/tcp       docker-redis-1

You should see logs like:

server1 | TLS server listening on port 65432
server2 | TLS server listening on port 65432
redis  | Ready to accept connections

Verify Docker stateless servers (Part 8)

Stop server1:

docker-compose stop server1


Restart it:

docker-compose start server1


✅ 1. Send a Chat Message (Lobby)
Hello everyone


If another user is connected in lobby, they will receive:

[2026-02-11 03:12:00] [alice] Hello everyone

✅ 2. Join a Room
/join room1


Expected response:

[time] Joined room room1

✅ 3. Leave Room
/leave

✅ 4. List Rooms
/rooms

✅ 5. See Connected Users
/who

✅ 6. Subscribe to Someone
/subscribe bob

✅ 7. Unsubscribe
/unsubscribe bob

✅ 8. Logout
/quit


Expected:

LOGOUT_SUCCESS


Then connection will close.

⚠ Important About OpenSSL

If you don't see server responses immediately:

Use this version:

openssl s_client -connect localhost:65432 -quiet


-quiet removes debug noise and makes it behave like a simple chat client.


Perfect timing 👍
Below is a **clean, examiner-friendly command checklist** to **verify Problems 1 → 8**, using **only terminal commands + client inputs**.
You can literally copy-paste this into your **README / testing section**.

---

# ✅ Assignment-1 Verification Commands (Problem 1–8)

Assume:

* Server terminal = **T1**
* Client terminals = **C1, C2, C3**
* Server running on `localhost:65432`

---

## 🔹 Problem 1 – Thread-Based TCP Chat Server

### Start Server

```bash
python src/server/server.py
```

### Open two clients (in different terminals)

```bash
python src/client/client.py
python src/client/client.py
```

### Expected server output

```
[CONNECT] TCP connection from (...)
[TLS] Handshake successful
```

✔ Multiple clients connect simultaneously
✔ One thread per client
✔ No server blocking

---

## 🔹 Problem 2 – Authentication (LOGIN)

### Client command

```text
LOGIN bob password456
```

### Expected response

```
AUTH_SUCCESS
```

### Invalid password test

```text
LOGIN bob wrongpass
```

Expected:

```
AUTH_FAILED
```

✔ bcrypt password hashing
✔ Session created only after AUTH_SUCCESS

---

## 🔹 Problem 3 – Duplicate Login Handling (Reject Policy)

### Open another client

```bash
python src/client/client.py
```

### Try logging in with same user

```text
LOGIN bob password456
```

### Expected

```
ERROR: User already logged in
[DISCONNECTED]
```

✔ Existing session untouched
✔ New login rejected

---

## 🔹 Problem 4 – Chat Rooms

### Default room test

After login:

```text
Hello lobby
```

### Join a room

```text
/join room1
```

Expected:

```
Joined room room1
```

### List rooms

```text
/rooms
```

Expected:

```
Rooms: lobby, room1
```

### Leave room

```text
/leave
```

Expected:

```
Returned to lobby
```

✔ Messages scoped per room
✔ Lobby default on login

---

## 🔹 Problem 5 – Publish–Subscribe Model

### Client A (alice)

```text
LOGIN alice password123
```

### Client B (bob)

```text
LOGIN bob password456
```

### Bob subscribes to Alice

```text
/subscribe alice
```

Expected:

```
Subscribed to alice
```

### Alice publishes

```text
Hello Bob
```

### Bob receives

```
[FROM alice] Hello Bob
```

### Unsubscribe test

```text
/unsubscribe alice
```

✔ Messages delivered **only to subscribers**
✔ Ordering preserved per publisher

---

## 🔹 Problem 6 – Redis Distributed State

### Start Redis

```bash
redis-server
```

### Verify sessions

```bash
redis-cli
HGETALL sessions
```

Expected:

```
bob server1
alice server1
```

### Verify room membership

```bash
SMEMBERS room:lobby
```

### Verify pub/sub

```bash
SUBSCRIBE global_chat
```

✔ Shared state in Redis
✔ Stateless server
✔ Redis Pub/Sub working

---

## 🔹 Problem 7 – TLS Encrypted Transport

### Observe server log

```
[TLS] Handshake successful
```

### Try plaintext connection (should fail)

```bash
openssl s_client -connect localhost:65432
```

Expected:

```
Connection closed / TLS error
```

✔ SSL/TLS enforced
✔ No plaintext allowed

---

## 🔹 Problem 8 – Dockerized Deployment

### Build & run

```bash
docker-compose up --build
```

### Verify containers

```bash
docker ps
```

Expected:

```
server_1
redis
```

### Test via client (outside Docker)

```bash
python src/client/client.py
LOGIN alice password123
```

✔ Redis + server auto-start
✔ Reproducible environment

---

## 🟢 Bonus: Graceful Server Shutdown

### Server terminal

```text
Ctrl + C
```

Expected:

```
[SERVER SHUTDOWN] Ctrl+C received. Stopping server...
```

✔ No traceback
✔ Threads exit cleanly

---

## 📌 Final Tip (for report)

Add this line in your PDF:

> “All eight problems were tested manually using multiple concurrent client terminals, Redis CLI verification, TLS handshake validation, and Dockerized deployment. Observed behavior matched expected outcomes.”

---

If you want, next I can:

* Convert this into a **README.md**
* Write the **2–4 page PDF report**
* Give you a **final polished server.py** ready for submission

Just say the word 👌
